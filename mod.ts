import * as proc from "https://deno.land/x/proc@0.19.7/mod.ts"

export function hello(): string {
  return "Hello. This is a placeholder.";
}

interface RunOptions {

}

export function<T> run(options?: Options, ...args: string[]): T {

} 